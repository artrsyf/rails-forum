%h1 Index page
- @post.each do |post|
  - if current_user.id == post.user_id
    %h2= link_to post.title, post
  - else
    %h2= post.title
  %p
    Published at
    =time_ago_in_words(post.created_at)
    by
    =post.user.email

