!!!
%html
  %head
    %title Forum
    %meta{name: "viewport", content: "width=device-width,initial-scale=1"}
    = csrf_meta_tags
    = csp_meta_tag

    = stylesheet_link_tag "application", "data-turbo-track": "reload"
    = javascript_importmap_tags

  %body
    %nav
      #logo
        %p Rails Forum
      #buttons
        =link_to 'New Post', new_post_path if (controller_name != 'sessions' && action_name != 'new') # ? *
    .wrapper
      %p.notice= notice
      %p.alert= alert
    -# tabs are really important in haml *
    -if user_signed_in?
      = link_to 'Sign out', destroy_user_session_path, data: { turbo_method: :delete }
    -else
      -#some block
      =link_to 'Sign in', new_user_session_path if (controller_name != 'sessions' && action_name != 'new') # ? *
    .wrapper
      = yield 